version: '3'
services:
  postgres:
    restart: always
    image: "postgres:10.4-alpine"
    environment:
      POSTGRES_DB: vetsis
      POSTGRES_USER: vetsis
      POSTGRES_PASSWORD: vetsis
    ports:
      - "5432:5432"
